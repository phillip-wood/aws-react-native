type UserSneaker @model @auth(rules: [{allow: private}]) {
  id: ID!
  user_id: String!
  condition: String!
  images: [String]
  Sneaker: Sneaker @connection
}

type Sneaker @model @auth(rules: [{allow: private, operations: [read, create]}]) {
  id: ID!
  brand: String!
  model: String!
  submodel: String
  colorway: String
}
 